---
description: Goal management
---
`/goal [cond]`: set + memory_learn + reflect_suggest. `/goal status`. `/goal check`: suggest if stalled.
`/goal done`: reflect(success) + episode. `/goal clear`: remove without reflect.
