# Workflows

Reserved for reusable workflow definitions. See `docs/workflows.md` for the
documented development workflow chain (brainstorming -> writing-plans ->
executing-plans -> finishing-branch). This directory is currently a placeholder.
